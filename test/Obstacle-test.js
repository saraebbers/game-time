const { assert } = require('chai');
const Obstacle = require ('../lib/Obstacle');

describe ('Obstacle', () => {
  it('should instanciate new obstacles', () => {
    //setup
    
    //execution
    //assertion
  })
  it.skip('it should start on the 0 or 600 x axis', () => {
    //setup
    //execution
    //assertion    
  })
  it.skip('it should move horizotally across the screen', () => {
    //setup
    //execution
    //assertion
  })
  it.skip('it should disappear when it hits a wall', () => {
    //setup
    //execution
    //assertion    
  })
  it.skip('it should console.log when colliding with frog', () => {
    //setup
    //execution
    //assertion    
  })
  it.skip('it should appear to move smooth like cool jazz axcross the screen', () => {
    //setup
    //execution
    //assertion    
  })
})